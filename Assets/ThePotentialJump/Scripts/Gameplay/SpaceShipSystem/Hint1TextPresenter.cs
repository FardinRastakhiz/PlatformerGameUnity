using SimpleJSON;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ThePotentialJump.Gameplay
{
    public class Hint1TextPresenter : TextPresenter
    {
        //[SerializeField] protected string id2;
        //[SerializeField] protected string id3;
        //public override void AlterLanguage(JSONNode langNode)
        //{
        //    string.Format("Lock the speed between {1} m/s and  160 m/s for 10 seconds before the 5000 m height", )
        //    textComponent.text = $"Lock the speed between 150 m/s and  160 m/s for 10 seconds before the 5000 m height";
        //}
    }


}
